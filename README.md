# config

Init ENV
```shell
# one line command
_PID=$$ && sudo apt update && sudo apt install -y git htop tmux vim wget && git clone https://github.com/postoor/config "$_PID"_config && sh "$_PID"_config/install.sh && rm -rf "$_PID"_config
```
